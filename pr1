mylist=[1,2,3,1,2,1,13,2,6,5]
length = len(mylist)
newLength = 0
while length != newLength:
    length = newLength
    result = mylist.remove(1)
    newLength = len(mylist)
    print(newLength, length)
